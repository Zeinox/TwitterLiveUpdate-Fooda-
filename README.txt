IDE: IntelliJ
Language: Java

I assumed the user would not request for a number of tweets beyond 200 pages, this can be changed.  It currently will just stop if it reaches 200 pages of tweets

I assumed that you wanted benign tweets and replys.

I also signed up for my own api key.

I also assumed that the user would strictly use the command line args in the format [user handle] [integer]